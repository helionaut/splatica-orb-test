# Calibration inputs

Store camera intrinsics, camera-to-camera extrinsics, camera-to-IMU extrinsics,
and IMU noise/bias parameters here once they are available.

Keep user-specific calibration exports out of git unless they are safe to share
and needed for a reproducible public fixture.
