# Fixture sequences

Small reproducible smoke fixtures belong here.

The HEL-43 harness ships only a directory placeholder so `make smoke` can prove
the repository layout and artifact generation path without requiring the user's
real recordings yet.
